name: First Workflow
on: workflow_dispatch
jobs:
  first-job:
    runs-on: ubuntu-latest
    steps:
      - name: Print greeting
        run: echo "Hello World!!"
      - name: Print goodbye
        run: echo "Goodbye world"
      - name: Print goodbye again
        run: echo "Goodbye world again"
